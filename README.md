# Dev NQR Bot

Bot Telegram đa năng: spam, tag, call, mod, bot con.

## Cài đặt

```bash
# 1. Cài Python 3.10+
# 2. Cài thư viện
pip install -r requirements.txt

# 3. Cấu hình
cp .env.example .env
nano .env  # điền BOT_TOKEN + OWNER_ID

# 4. Chạy
python main.py